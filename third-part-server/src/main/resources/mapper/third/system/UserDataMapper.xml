<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinyafu.jmall.mapper.third.system.UserDataMapper">

    <select id="queryList" parameterType="com.jinyafu.jmall.entity.data.system.UserDTO" resultType="com.jinyafu.jmall.entity.data.system.UserData">
        select * from user
        <where>
            <if test="data.name != null and data.name != ''">and name like "%"#{data.name}"%"</if>
        </where>
    </select>
</mapper>