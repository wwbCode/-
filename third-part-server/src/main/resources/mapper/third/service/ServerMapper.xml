<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinyafu.jmall.mapper.third.service.ServerMapper">

    <select id="queryList" parameterType="com.jinyafu.jmall.entity.data.service.ServerDTO" resultType="map">
        select * from s_server
        <where>
            <if test="data.projectName != null and data.projectName != ''">and projectName like "%"#{data.projectName}"%"</if>
        </where>
    </select>

    <select id="list" resultType="map">
        select * from s_server
    </select>
    
    <insert id="insertServers" parameterType="com.jinyafu.jmall.entity.third.service.Server">
        <selectKey keyProperty="id" resultType="String" order="BEFORE">
            select replace(uuid(),'-','') as id
        </selectKey>
        insert into s_server
        (
        id,
        projectName,
        machineryRoom,
        intranetIp,
        otherIp,
        fixedAssetsCode,
        purpose,
        equipmentType,
        account,
        osType,
        loginType,
        remark
        )
        values
        (
        #{id},
        #{projectName},
        #{machineryRoom},
        #{intranetIp},
        #{otherIp},
        #{fixedAssetsCode},
        #{purpose},
        #{equipmentType},
        #{account},
        #{osType},
        #{loginType},
        #{remark}
        )
    </insert>

    <update id="update" parameterType="com.jinyafu.jmall.entity.third.service.Server">
        update s_server
        set
        projectName = #{projectName},
        machineryRoom = #{machineryRoom},
        intranetIp = #{intranetIp},
        otherIp = #{otherIp},
        fixedAssetsCode = #{fixedAssetsCode},
        purpose = #{purpose},
        equipmentType = #{equipmentType},
        account = #{account},
        osType = #{osType},
        loginType = #{loginType},
        remark = #{remark}
        where id = #{id}
    </update>

    <select id="get" parameterType="string" resultType="com.jinyafu.jmall.entity.third.service.Server">
        select * from s_server where id = #{id}
    </select>

    <delete id="delete" parameterType="string">
        delete from s_server where id = #{id}
    </delete>
</mapper>